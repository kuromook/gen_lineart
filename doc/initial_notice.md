# Track B: SDXL路線 — 下絵との乖離(条件忠実度)

作成: 2026-09-06 JST / 更新: 2026-09-08 / ブランチ: `controlnet-sdxl-fidelity`
前身: `lineart-controlnet-realpairs`(クロスハッチ脱却を達成して終了)
起案: `doc/track_proposal_20260906.md`(このtrackにも同梱)
前身の全経緯: `doc/track_controlnet_realpairs_work_log.md`

このファイルは「現状・次の一手・運用ルール」だけを短く保つ。
実験の時系列は`doc/work_log.md`に追記すること。

## 現状 (2026-09-08 時点)

**起案時の課題設定は測定で全面的に書き換わり、さらに1024での再学習も
失敗に終わった。** 以下はすべて測定済みの事実(`doc/work_log.md` 2026-09-06 /
09-08 の各項)。

### 起案時の前提のうち、覆ったもの

- 「**SDXLはcsを上げると悪化する**(csレバーが効かない)」は**誤り**。
  それは`sdxl_trained`(=512学習のanime LoRA)1本の性質だった。
- 「**SDXLは下絵との乖離が激しい**」も**否定**。乖離していたのは
  512で学習したLoRAであって、SDXLでも素のControlNetでもない。

### 到達点: 学習しないのが最良 (f1 0.2582)

`Eugeoter/noob-sdxl-controlnet-lineart_anime` を **LoRAなし・1024・cs2.5**で
推論する構成。f1 0.2582 / ink 0.0447 / 線幅 3.06 / near_white 77.6%
(GT: 0.0353 / 3.72 / 94.8%)。前trackの11モデル最良(0.2337)を上回る。

推論が読むのは**既製の学習済みモデル2つ + 下絵1枚 + 固定キャプション**だけ。
**ペアデータも学習も、この出力には一切寄与していない。**

### 1024での再学習は失敗 (2026-09-08完了)

50.3時間・21,160ステップを完走(peak VRAM 8.20GiBで安定、損失も微減)。
学習自体は健全だったが、**全軸で素のControlNetに劣った**:

| | f1 | ink | 線幅 | near_white | midtone |
|---|---:|---:|---:|---:|---:|
| bare cs2.5 | **0.2582** | 0.0447 | **3.06** | **77.6%** | 9.6% |
| finetuned cs2.0(最良) | 0.1602 | 0.1781 | 13.87 | 13.6% | 68.0% |

delta -0.0980。**512学習と同じ失敗モードが1024でも再現。解像度は原因では
なかった。** 「1024でターゲットがぼける」仮説も測定して棄却済み
(拡大してもnear_white 93.9%→93.8%、midtone 2.7%→3.1%)。

**結論: ε-MSEでのfine-tuneは、この対データでは素のControlNetを改善しない。**
独立した3ラン(512 anime / 512 manga / 1024 anime)がすべて同じ方向に劣化。
しかも損失は下がっているので、**目的関数は満たされたまま欲しいものが
悪化している**。

### ペアデータの位置づけ(整理)

| 用途 | 現状 |
|---|---|
| 学習データとして | **不要と判明**(3ラン全部で悪化) |
| **測定器として** | **不可欠のまま** — cs2.5の決定も、グレー地の検出も、
  fine-tuneの劣化判定も、すべてGTと突き合わせて初めて可能だった |

残差(下絵のアタリ線まで残した薄いスケッチ vs GTの取捨選択された線)も
GTがあって初めて見える。将来その差を埋める手段が見つかるなら、
学習信号はペアからしか取れない。

### 最大の弱点: すべて5タイルでしか測っていない

上記の結論はすべて`data/diag_valid5.txt`の**5枚**が根拠。前trackの
11モデル表も同じ5枚なので比較としては公正だが、5枚は薄い。
**「既製品が全学習済みモデルを上回る」という重い結論を5枚に載せたままに
しない**こと。→ 次の一手#1。

## 次の一手(優先度順)

### 1. 【最優先・準備完了・金曜に起動】ホールドアウト292タイルでの再検証

**ユーザー判断(2026-09-08): 金曜までGPUは別trackが使用。この検証は
週末に行う。** スクリプトと評価タイルは確定済みなので、**金曜は起動するだけ**:

    ./experiments/run_holdout_validation_20260912.sh

- **推論のみ。学習はしない。** 所要 約8.1時間 + 前処理10分
- セル単位で`.complete`を持つので中断しても再実行で再開する
- 失敗時は通知が飛ぶ(`logs/holdout_validation_20260912.failed`)

**2群に分けて別々に採点する**(規模拡大と出典変更を交絡させないため。
共通基盤のtest splitは housei 1,596 + lineart_004 24 という偏った構成で、
無作為抽出すると出典が変わってしまう):

- **群A: `dataset/pairs_480/holdout_lineart_family.txt` 192タイル** —
  診断5枚と**同系統**の出典(共通基盤train split 168 + test split 24)。
  **我々の学習リスト8,467件との重複0件を検証済み**なのでfine-tuneは
  1枚も見ていない。**主問: 5枚の結論は192枚でも成り立つか**
- **群B: `dataset/pairs_480/holdout_housei_100.txt` 100タイル** —
  別出典(seed 20260912固定)。副問: 系統を越えて一般化するか。
  Aと違う結果でも矛盾ではなく、一般化に関する所見として別に報告する

**診断5枚は群Aに含まれている**ので、同じランの中で旧数値
(bare cs2.5 = 0.2582、ft cs2.0 = 0.1602)を再現するか**アンカー照合**が
成立する。ズレたら段取りか前処理が過去と違うので、他の数値は読まない。

測るのは bare cs2.0/2.5/3.0(プラトーの確認) と ft cs2.0(最良セル)の4構成。
採点は`experiments/score_holdout_validation_20260912.py`が
アンカー→群A→群Bの順に出力し、**平均だけでなく中央値と標準偏差**も出す
(292枚の平均は二峰性を隠せるため)。

### 2. 検証の結果次第で分岐

- **5枚の結論が保たれた場合** → 「SDXL路線の到達点は既製ControlNetの
  1024/cs2.5であり学習は不要」が確定。課題は残差である
  **線の取捨選択と強弱づけ**に移る。これはfine-tuneでは届かなかった領域
  なので、別の手段(後処理・リファイナ・別の学習信号)の検討になる
- **5枚が偏りだった場合** → 方針判断そのものをやり直す

### 3. (未着手・優先度低)なぜ小さなLoRAが条件追随を壊すのか

機構は未解明。候補は**訓練/推論の乖離** — 学習時はcs=1.0・CFGなしでしか
見ていないのに、推論はcs2.5・guidance5.0で行う。検証するなら
「cs=1.0・guidance=1.0での評価」で差が縮むかを見るのが最小の実験。
ただし#1の結果次第では追う価値がなくなる可能性がある。

### 4. 条件画像側の伸びしろ

2×2で**`cnAnime` × `condManga`の1024が f1 0.2438**と、同ControlNetに
coarseを与えた0.2341を上回った。前trackは各ControlNetにその前処理を
組み合わせる流儀だったので、この交差は未探索。

## 参照

- ベース: `animagine-xl-3.1`
  (`/home/sh1/.cache/huggingface/hub/models--cagliostrolab--animagine-xl-3.1/...`)
- ControlNet init: `Eugeoter/noob-sdxl-controlnet-lineart_anime`(採用) および
  `...-manga_line`(不採用、上記)
- 既存チェックポイント(512学習、劣ることが確定済み。歴史的アンカー用):
  `checkpoints/controlnet_lora_sdxl_20260829/final`、
  `..._sdxl_manga_20260830/final`
- データ: `data/`(約1GB、`.gitignore`対象)。前trackから`cp -a`で複製済み
  (2026-09-06)。`train_list.txt` 8,467件、`line/`、`rough_lineart_coarse/`、
  `rough_manga_line/`、`captions.csv`ほか。
  v3プール(8,798タイル)には**移行しない**方針(ユーザー判断、2026-09-06)。
- 1024学習用キャッシュ: `data/cache_sdxl_1024/`(約4.9GB、`.gitignore`対象、8,467件生成済み)。
  `data/line`のlatentとテキスト埋め込みのみ。条件画像に依存しないので
  anime/manga両variantで共用できる。

## 運用ルール

- 大きな学習・抽出は必ずスモークテストしてから本番投入
  (数日規模のバッチが引数ミスで落ちるのが最も高くつく失敗)
- **長時間GPUジョブは月〜木の4日連続バッチが標準枠**(ユーザーは平日ほぼ
  在室しない)。週末は対話的な作業——レビュー・分析・短い実験・次の
  長時間バッチを決めること——に充てる。数日かかること自体は問題ない。
  チェックポイントは3日目のクラッシュで全損しない頻度で取る
- バックグラウンド実行は`setsid`で完全に切り離す(PPID=1を確認)。
  `pkill -f <スクリプト名>`は**自分のシェルのコマンドラインにも一致して
  自滅する**ので、PIDを特定して`kill`すること(2026-09-06に踏んだ)
- 数値指標だけで判断しない、必ず目視モンタージュを作り、**その画像の
  正確なパスを数値と一緒に併記する**。画像は`/tmp`ではなく
  **作業フォルダ(`results/`)に置く**(ユーザー要望、2026-09-06)
- **モデル評価を単一の`controlnet_conditioning_scale`だけで行わない**。
  さらに**解像度とcsは交互作用する**ので、片方だけ振っても効果は見えない
- **`orientation_entropy`単体で判断しない**。`line_width_p50`(GT≈3.7)と
  `ink_ratio`(GT≈0.035)を必ず併記する
- **`gt_bsds_f1`単体でも判断しない**。紙の白さを一切見ていない。
  `bg_mode`/`near_white_frac`/`midtone_frac`(GT: 255 / 94.8% / 1.8%)を
  必ず併記する。実例2件——灰色一色の`anime_base` 512/cs1.0が
  スイープ最良のf1 0.2263を白3.1%で取り、ほぼ空白の`manga_base`
  1024/cs1.0が0.2121を白87.8%で取った
- **`near_white`単体でも判断しない**。「きれいだから白い」のか
  「描けていないから白い」のかを区別できない。線幅と目視で裏を取る
- 実験の結論が出たら、その場で`results/`の生成物を要否判断する

## 金曜(2026-09-12)再開時のチェックリスト

1. GPUが空いているか: `nvidia-smi --query-compute-apps=pid,used_memory --format=csv`
2. 起動: `setsid ./experiments/run_holdout_validation_20260912.sh > /dev/null 2>&1 < /dev/null &`
   (PPID=1を確認。`pkill -f`は使わない——運用ルール参照)
3. 進捗: `tail -3 logs/holdout_validation_20260912.log`
4. 失敗検知: `logs/holdout_validation_20260912.failed` が存在すれば失敗
5. 完了後に見るもの:
   - `results/holdout_validation_20260912/scores.csv`(アンカー/群A/群B)
   - `results/holdout_validation_20260912/montage_holdout.png`(12タイル)
   - **アンカー照合から読むこと。** 5枚の再測定が
     bare cs2.5 = 0.2582 / ft cs2.0 = 0.1602 から離れていたら、
     段取りが過去と違うので他の数値は読まない

準備済みで金曜に不要な作業:
- 評価タイル292件は確定済み(`dataset/pairs_480/holdout_*.txt`)、
  rough/GTが全件そろうことを検証済み
- 前処理スクリプトは`tools/preprocess_lineart_coarse_condition.py`に
  取り込み済み(前trackへの依存を解消)
- スクリプトは`bash -n`/`ast.parse`で静的検査済み
