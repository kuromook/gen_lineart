# Notebooks

This directory keeps exploratory notebooks that are useful as historical
references but are not the current executable source of truth.

- `gen_lineart.ipynb`: legacy architecture and early edge-loss design notes.
  Current model and training code live in `lineart/` and `scripts/`.
